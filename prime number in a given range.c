#include<stdio.h>
void main()
{
    int n,i,count=0,m,a;
    printf("enter number:");
    scanf("\n%d %d %d",&a,&n,&m);
    for(i=m;i<=n;i++)
    {
        if()
        {
            
            
        }
    }
    if(count==2)
    {
        printf("prime number");
    }
    else
    {
        printf("not a prime number");
    }
    
}